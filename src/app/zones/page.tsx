import { ComingSoon } from "@/components/coming-soon";

export default function ZonesPage() {
  return (
    <ComingSoon
      title="The H-zone paradox"
      phase="Phase 6"
      description="H-zone is anatomically high-risk, yet H-zone tumours require fewer sections than L-zone. Why? L-zone tumours are ~6× larger. Interactive scatter explores this counterintuitive finding."
    />
  );
}
