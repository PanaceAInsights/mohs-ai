import { ComingSoon } from "@/components/coming-soon";

export default function WhyPage() {
  return (
    <ComingSoon
      title="Why the model decides what it decides"
      phase="Phase 5 · SHAP"
      description="SHAP summary and dependence plots. The 1.5 cm² tumour-area threshold — the paper's key clinical finding — overlaid visually. Per-case waterfall from the predictor."
    />
  );
}
